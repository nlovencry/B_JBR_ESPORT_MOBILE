<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apkgeming" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApkGeming"
        android:usesCleartextTraffic="true" >
        <activity android:name=".DetailJadwal" />
        <activity android:name=".jadwal" />
        <activity android:name=".Detail_event" />
        <activity android:name=".event" />
        <activity android:name=".editprofile" />
        <activity android:name=".tab_login" />
        <activity android:name=".MainActivity2" />
        <activity android:name="com.Auth.LoginActivity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Profile" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>